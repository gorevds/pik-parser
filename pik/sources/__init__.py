"""Источники данных по застройщикам кроме ПИК.

У каждого застройщика свой сайт/API — общего нет. Каждый модуль здесь
приводит свой формат к нормализованным `NormBlock`/`NormFlat` (см. base.py),
а `build_rows` превращает их в строки таблиц blocks/flats/snapshots.
"""
